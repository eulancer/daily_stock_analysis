# .github/workflows/keep-alive.yml
name: Keep Repository Active
on:
  schedule:
    - cron: '0 */6 * * *'  # 每6小时运行一次
  workflow_dispatch:

jobs:
  keep-alive:
    runs-on: ubuntu-latest
    steps:
      - run: echo "保持仓库活跃状态 - $(date)"
